<%# <%= render(CourseComponent.with_collection(Course.includes(:units).all)) %>

<%# <%= link_to render(:partial => 'course'), root_url %>
<%# <%= render(CourseComponent.new(title: "Title", volume: "Volume", price: "Price")) %> 

<%= render(CourseComponent.with_collection(@courses)) %>
 <%# <%= link_to render(:partial => 'course'), root_url %> 
